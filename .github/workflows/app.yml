name : Infralens

on :
  push:
    branches : [main]
jobs :
   infra :
      runs-on: ubuntu-latest

      steps:
        - name : checkout code
          uses : actions/checkout@v4

       # - name : login to dockerhub
       #   uses : action/login-action@v3
          

  
    
